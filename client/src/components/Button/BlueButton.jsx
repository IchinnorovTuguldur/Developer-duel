import { BlueButton } from "./Button.styles";
const StyledBlueButton = ({children}) => {

    return (
        <BlueButton>
            {children}
        </BlueButton>
    )
}

export default StyledBlueButton;